services:
  - type: web
    name: sure-seal-sfa
    env: node
    buildCommand: |
      npm install
      npm run build
      cd server
      npm install
    startCommand: cd server && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000